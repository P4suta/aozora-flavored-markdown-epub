# Tighter than clippy's defaults.
too-many-arguments-threshold   = 4
too-many-lines-threshold       = 80
cognitive-complexity-threshold = 18
max-fn-params-bools            = 1
pass-by-value-size-limit       = 128
